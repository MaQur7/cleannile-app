import PageLoader from "../../../components/ui/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading reports"
      description="Preparing form tools for report submission."
    />
  );
}
