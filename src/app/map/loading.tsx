import PageLoader from "../../components/ui/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading map"
      description="Initializing map tiles and report geometry."
    />
  );
}

