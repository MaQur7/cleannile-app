import PageLoader from "../../components/ui/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading moderation"
      description="Preparing pending reports and moderation queue."
    />
  );
}

