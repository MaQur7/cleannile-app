import PageLoader from "../../components/ui/PageLoader";

export default function CaptureLoading() {
  return (
    <PageLoader
      title="Preparing field capture"
      description="Loading fast reporting controls and location tools."
    />
  );
}
