import PageLoader from "../../components/ui/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading dashboard"
      description="Preparing control panel modules and quick actions."
    />
  );
}

